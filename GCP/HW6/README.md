==Start w/ search
Lots of validation and inline overlay alerts
if check box, then instant clear form
Clear clears everything including all the cards

==when submit, simply add current weather below in "card" style (and add multiple days of weather below that)
For homework, must use JSON data back and forth, and then use JS to populate the HTML on the client side.
What if I hard code the HTML and then use JS to fill in fields? Yes


==when I click a day in the forecast, the current cards get replaced with a Daily Weather Details and Weather Charts (button)??
Hovering over the charts displays all the info